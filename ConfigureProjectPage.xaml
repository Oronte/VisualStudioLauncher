<Page x:Class="VisualStudioLauncher.ConfigureProjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VisualStudioLauncher"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConfigureProjectPage">

    <Page.Resources>
        <Style x:Key="SelectableButton" TargetType="Button">

            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="Border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">

                            <ContentPresenter 
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4D4D4D"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </Page.Resources>
    
    <Grid>
        <StackPanel Orientation="Vertical" Background="Transparent" Margin="50, 100, 350, 150">
            <TextBlock Text="Project name" Foreground="White"/>
            <local:SearchBarWidget x:Name="projectName"/>
            <TextBlock Text="Location" Foreground="White"/>
            <StackPanel Orientation="Horizontal">
                <local:SearchBarWidget x:Name="location"  Width="375"/>
                <Button Content="..." Margin="5, 5, 5, 5" Width="20" Background="#2E2E2E" Foreground="White" Click="SearchProjectLocation"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="360,398,-78,10">
            <Button x:Name="BackButton" Content="Back" Foreground="White" Background="#3D3D3D" Width="100" Height="40" Margin="300,0,10,0" HorizontalContentAlignment="Center" Style="{StaticResource SelectableButton}" Click="BackButton_Click"></Button>
            <Button x:Name="NextButton" Content="Next" Foreground="White" Background="#3D3D3D" Width="100" Height="40" HorizontalContentAlignment="Center" Style="{StaticResource SelectableButton}" Click="CreateProject"></Button>
        </StackPanel>
    </Grid>
</Page>
