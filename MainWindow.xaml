<Window x:Class="VisualStudioLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisualStudioLauncher"
        mc:Ignorable="d"
        Title="Visual Studio Launcher"
        Height="520"
        Width="920"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        BorderThickness="2"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="10"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border CornerRadius="10"
            Background="#1E1E1E"
            BorderBrush="MediumPurple"
            BorderThickness="1">

            <Grid Height="42" VerticalAlignment="Top" Background="#2D2D30" MouseDown="DragWindow">
                <TextBlock Text="Visual Studio"
                Foreground="White"
                VerticalAlignment="Center"
                Margin="15, 2"
                FontSize="13"
                FontWeight="SemiBold"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Width="45" Background="Transparent" Foreground="White" BorderBrush="Transparent" Style="{StaticResource RoundedButtonStyle}"/>
                    <Button Content="□" Width="45" Background="Transparent" Foreground="White" BorderBrush="Transparent" Style="{StaticResource RoundedButtonStyle}"/>
                    <Button Content="✕" Width="45" Background="Transparent" Foreground="White" BorderBrush="Transparent" Style="{StaticResource RoundedButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
