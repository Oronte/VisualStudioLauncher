<Window x:Class="VisualStudioLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisualStudioLauncher"
        mc:Ignorable="d"
        Title="Visual Studio Launcher"
        Height="677"
        Width="1020"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        BorderThickness="2"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="MainButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGray"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MainQuitButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border x:Name="mainWindowBorder" CornerRadius="10"
            Background="#1E1E1E"
            BorderBrush="MediumPurple"
            BorderThickness="1">

            <Grid Height="42" VerticalAlignment="Top" Background="Transparent" MouseDown="DragWindow">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="-" Width="45" Style="{StaticResource MainButton}" Click="Minimize"/>
                    <Button Content="□" Width="45" Style="{StaticResource MainButton}" Click="Maximize"/>
                    <Button Content="✕" Width="45" Style="{StaticResource MainQuitButton}" Click="Close"/>
                </StackPanel>
            </Grid>

        </Border>
        <Grid Margin="30, 45, 30, 45">
            <Frame x:Name="frame" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>
