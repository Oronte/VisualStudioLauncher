<Page x:Class="VisualStudioLauncher.TemplatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VisualStudioLauncher"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TemplatePage">

    <Page.Resources>
        <Style x:Key="SelectableButton" TargetType="Button">

            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                            <ContentPresenter 
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4D4D4D"/>
                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*"/>
            <ColumnDefinition Width="58*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.Row="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Content="Create a new project"
               Foreground="White"
               FontSize="24"
               VerticalAlignment="Top" Margin="-15,-30,10,0"/>

            <Label
               Content="Recent project templates"
               Foreground="White"
               FontSize="20"
               Margin="-10,0,30,0"
               VerticalAlignment="Center" Height="40" Grid.Row="1"/>

            <ItemsControl Grid.Row="2"
                 ItemsSource="{Binding RecentsTemplate}"
                 Margin="-15,0,30,20"
                 Background="Transparent"
                 BorderBrush="Transparent">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:RecentTemplateWidget />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <Grid Grid.Column="1" Grid.Row="0" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <local:SearchBarWidget Grid.Row="0" Placeholder="Search for templates" Width="400" HorizontalAlignment="Left" Margin="0,-10,0,0"/>

            <ItemsControl Grid.Row="2"
                 ItemsSource="{Binding RecentsTemplate}"
                 Margin="-30,0,0,20"
                 Background="Transparent"
                 BorderBrush="Transparent">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:TemplateWidget />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <!--<Border Grid.Row="0" BorderBrush="#4A4A4A" BorderThickness="1.5" Background="#3D3D3D" VerticalAlignment="Top" Height="25" HorizontalAlignment="Left" Width="Auto">
                <TextBox HorizontalAlignment="Left" VerticalAlignment="Center" Text="OOOOOOH" Width="400" Background="Transparent" Foreground="White" BorderBrush="Transparent"></TextBox>-->
            <!--</Border>-->
        </Grid>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
            <Button x:Name="BackButton" Content="Back" Foreground="White" Background="#3D3D3D" Width="100" Height="40" Margin="300,0,10,0" HorizontalContentAlignment="Center" Style="{StaticResource SelectableButton}"></Button>
            <Button x:Name="NextButton" Content="Next" Foreground="White" Background="#3D3D3D" Width="100" Height="40" HorizontalContentAlignment="Center" Style="{StaticResource SelectableButton}"></Button>
        </StackPanel>
    </Grid>
</Page>
