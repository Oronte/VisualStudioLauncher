<UserControl x:Class="VisualStudioLauncher.TemplateWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VisualStudioLauncher"
             mc:Ignorable="d" 
             VerticalAlignment="Top"
             Height="Auto"
             Width="500">

    <UserControl.Resources>
        <Style x:Key="SelectableButton" TargetType="Button">

            <Setter Property="Background" Value="#242424"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="Center"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- 🔘 HOVER -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                            </Trigger>

                            <!-- ✅ SELECTED -->
                            <Trigger Property="Tag" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="DarkGray"/>
                            </Trigger>

                            <!-- 🔥 SELECTED + HOVER (priorité) -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Tag" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>

                                <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="DarkGray"/>
                            </MultiTrigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Button Background="#242424" Width="500" HorizontalAlignment="Left" HorizontalContentAlignment="Left" Style="{StaticResource SelectableButton}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"></ColumnDefinition>
                    <ColumnDefinition Width="*" ></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Image x:Name="TemplateIcon" Grid.Column="0" Width="30" Height="30" VerticalAlignment="Top" Margin="0,7" Source="{Binding Icon}" />
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Content="{Binding Name}" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize ="14" ></Label>
                    <TextBlock Grid.Row="1" Text="{Binding Description}" TextWrapping="Wrap" Foreground="White" FontSize="12"></TextBlock>
                    <Border Grid.Row="2" CornerRadius="5" BorderBrush="Black" BorderThickness="1" Background="#3D3D3D" VerticalAlignment="Center" HorizontalAlignment="Left" Width="Auto" Height="20" Margin="0,10">
                        <Label Content="{Binding Language}" FontSize="11" Foreground="White" HorizontalAlignment="center" VerticalAlignment="Center" BorderBrush="Transparent" Margin="0,-4,0,0"></Label>
                    </Border>
                </Grid>
            </Grid>
        </Button>
    </Grid>
</UserControl>
